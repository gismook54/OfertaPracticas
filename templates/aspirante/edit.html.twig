{% extends 'base.html.twig' %}

{% block title %}Edit Aspirante{% endblock %}

{% block body %}
    <h1>Edit Aspirante</h1>

    {{ include('aspirante/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('aspirante_index') }}">back to list</a>

    {{ include('aspirante/_delete_form.html.twig') }}
{% endblock %}
