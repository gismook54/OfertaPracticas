{{ form_start(form) }}
    {{ form_widget(form) }}
    <br />
    <div style="float:right;" class="row ">
        <br/>
        <a class="btn btn-primary btn-sm" href="{{ path('oferta_practica_profesional_index') }}">Cancelar</a>
        <button class="btn btn-primary btn-sm active">{{ button_label|default('Crear') }}</button>
    </div>
    <br />
{{ form_end(form) }}
